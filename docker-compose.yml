services:
  app:
    build: .
    container_name: wm-tipp-app
    restart: unless-stopped
    env_file: .env
    ports:
      - "3000:3000"
    volumes:
      - wm-data:/app/data

volumes:
  wm-data:
